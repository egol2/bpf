# SPDX-License-Identifier: GPL-2.0
#
# Makefile for SATerm syscall support
#
# Currently just the syscall implementation object. Extend here as the
# feature grows (e.g., helper code, core logic files, etc.).
#
# Built unconditionally so the syscall symbol is always present once
# the table entry is added.

obj-y := saterm.o
